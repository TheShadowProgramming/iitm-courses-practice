def twoLevelSort(scores):
    min = -1
    LoneSorted = []
    while len(LoneSorted) < len(scores):

        for i in scores:
            if i[1] < min:

                scores
                min = i[1]
